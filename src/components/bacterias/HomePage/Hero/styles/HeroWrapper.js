import styled from 'styled-components';
const HeroWrapper = styled.header`
    box-shadow:inset 0 0 5rem 1rem hsla(0,100%,0%,.7);
    height:calc(100vh - 20rem);
    position:relative;
    max-width:100vw;
    min-width:100vw;
    overflow-x:hidden;
    width:100vw;
`
export default HeroWrapper;